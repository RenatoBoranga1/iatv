# Configuração
A URL da API Android vem da propriedade Gradle `IATV_API_URL`. Configuração do servidor está em `.env.example`; o Compose lê `.env` local ignorado pelo Git. Não colocar credenciais no APK.
