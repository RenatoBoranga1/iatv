# Tipos compartilhados
Os contratos TypeScript estão em `packages/contracts`. O cliente Kotlin espelha os DTOs de rede em `core/model`. A API `/docs-json` fornece OpenAPI para geração futura. Não há compartilhamento binário entre Kotlin e TypeScript.
